<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8'>
    <title>Thank You!</title>
  </head>
  <body>
    <h1>Thanks <%= name %>!</h1>
    <p>Thanks for coming to our conference. We couldn't have done it without you!</p>

    <p>
      Political activism is at the heart of any democracy and your voice needs to be heard.
      Please consider reaching out to your following representatives:
    </p>

    <table>
    <!--Header Rows-->
      <tr>
        <th>Legislators</th>
        <th>Websites</th>
      </tr>
    <!--Create a table row for each legislator for the attendee-->
      <% if legislators.kind_of?(Array) %>
        <% legislators.each do |legislator| %>
          <tr>
            <td><%= "#{legislator.name}" %></td>
            <td>
            <!--If a legislator has more than one website, list them in a <ul> that gets nested inside a <td>-->
              <% if legislator.urls.length > 1 %>
                <ul>
                  <% legislator.urls.each do |url| %>
                    <li><%= "#{url}" %></li>
                  <% end %>
                </ul>
              <% else %>
                <%= "#{legislator.urls}" %>
              <% end %>
            </td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td><%= "#{legislators}" %></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
